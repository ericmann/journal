# golangci-lint configuration.
#
# CI builds golangci-lint from source with the repo's Go toolchain (see the
# `install-mode: goinstall` step in .github/workflows/ci.yml), so it can
# typecheck this go1.26 module. Default linters; just a generous timeout.
run:
  timeout: 5m
